# ASM 开发模式：自定义调用 ABI（stack-based, callee-cleans-stack）

## 核心思想

不使用 System V AMD64 的寄存器传参 ABI，而是自定义一套更简单、心智负担更低的调用约定：

- **参数**：调用方通过 `push` 依次压栈传参（而非放入 rdi/rsi/rdx/...），且按参数声明顺序正向 push —— **第一个参数先 push，最后一个参数最后 push**。因此进入函数后 `[rbp+16]` 对应的是*最后一个参数*（最靠近返回地址），第一个参数反而在最大的偏移处（详见「参数与局部变量访问约定」）。
- **栈清理**：被调用方（callee）通过 `ret N` 自行清理栈上的参数（类似 stdcall，而非 cdecl）。`N` = 参数总字节数（不含局部变量）。
- **返回值**：统一放在 `rax`。
- **变量一律落栈**：所有变量——不管是参数还是函数内部的局部变量——都只以栈上内存单元的形式存在，通过 `%define` 起别名访问（`[rbp + 偏移]` 或 `[rbp - 偏移]`）。**寄存器里永远不存变量**，只用来承载一次表达式求值过程中的中间结果，算完立刻写回栈上的变量，绝不让一个寄存器的值跨越到下一条语句、更不用说跨越一次 `call`。
- **调用会破坏所有寄存器**：不管是调用自定义 ABI 的函数，还是调用外部真实 ABI 的函数（如 libc 的 `printf`），一律默认——**除 `rax`（返回值）外，其余所有寄存器在 `call` 之后都视为已被破坏、值不可信**。因为变量本来就不放在寄存器里，这条规则不会造成任何负担：你不会有"调用前需要保护某个寄存器"的场景，因为需要在调用后还能用到的值，从一开始就该是栈上的变量，而不是寄存器里的临时值。

这套"调用清空所有寄存器"的假设对自定义 ABI 函数之间的调用、以及对外部真实 ABI 函数的调用都统一适用，因此**不需要任何寄存器保护宏**——无论是保存"局部要用的寄存器"，还是跨真实 ABI 边界时保护"caller-saved 寄存器"，这套设计里都不存在，直接删除即可。写函数时只需用 `begin` / `end` 两个宏建帧、收尾，中间该用哪个寄存器算表达式就用哪个，用完就扔。

## 两个宏：begin / end

```asm
%macro begin 0
    push rbp
    mov  rbp, rsp
%endmacro

%macro end 0
    mov  rsp, rbp
    pop  rbp
%endmacro
```

- `begin` 只做一件事：建立标准栈帧（`push rbp` + `mov rbp, rsp`）。因为寄存器不再需要保存/恢复，这个宏不用像原来的 `beginfn` 那样接受变长寄存器列表——固定两行，没有参数。
- `end` 对称地做收尾：`mov rsp, rbp` 把 `sub rsp, N` 分配的局部变量空间和调用真实 ABI 时垫的对齐空间一次性归还，再 `pop rbp` 恢复调用方的帧指针。`end` 之后紧跟 `ret N` 清理参数。
- 局部变量的 `sub rsp, N` 写在 `begin` 之后、函数体之前，由程序员按下面的对齐规则手算 `N`；`begin`/`end` 本身不管这件事。

## 参数与局部变量访问约定

### 栈帧布局

进入函数、`begin` 建好帧之后，栈布局固定为：

```
[rbp - 8*m]               局部变量 m （离 rbp 最远的局部变量）
...
[rbp - 16]                局部变量 2
[rbp - 8]                 局部变量 1 （最靠近 rbp）
[rbp + 0]                 旧 rbp
[rbp + 8]                 返回地址（call 压入）
[rbp + 16]                pn      （最后一个参数，最后 push，离返回地址最近）
[rbp + 24]                p(n-1)
...
[rbp + 16 + 8*(n-1)]      p1      （第一个参数，最先 push，离返回地址最远）
```

也就是说：

- **参数**在正偏移一侧，从最后一个参数往前数：最后一个参数固定在 `[rbp+16]`，往前每多一个参数偏移 +8，第一个参数在最大偏移处。
- **局部变量**在负偏移一侧：第 1 个声明的局部变量在 `[rbp-8]`，第 2 个在 `[rbp-16]`，依此类推。函数入口用 `sub rsp, N` 为它们预留空间；函数收尾时 `end`（`mov rsp, rbp`）一次性归还（不需要、也不用手动 `add rsp` 抵消）。

不管是参数还是局部变量，写 `%define` 别名时，书写顺序都按**声明顺序**（第一个写在最前面），不用跟着偏移正负方向排——只是每个别名对应的具体偏移值要按上面的规则算。函数体内直接写别名读写即可，可读性接近高级语言里的具名变量。

### `sub rsp, N` 要按 16 字节对齐算，不能只看局部变量个数

`N` **不等于**简单的「局部变量个数 × 8」——还要把参数 frame 的大小一起算进去，凑够 16 字节对齐，否则函数体内后续调用真实 ABI 函数（如 `printf`）时会因为 `rsp` 没对齐而出错。算法：

1. 设 `n` = 参数个数，`L` = 实际声明的局部变量个数。
2. `begin` 执行完之后，栈上已经有「返回地址(8) + 旧 rbp(8) + 参数(8n)」= `16 + 8n` 字节，**只要 `n` 是偶数，此时就已经 16 字节对齐**；`n` 是奇数则差 8 字节。
3. 分配局部变量空间时，实际要 `sub rsp` 的槽位数 `L'`（`L' >= L`，多出来的是纯占位、不对应任何变量）要满足：**`n + L'` 为偶数**。也就是说：
   - `n` 为偶数：`L'` 必须是偶数——`L` 是偶数就直接用 `L`（`N = 8L`）；`L` 是奇数就要多垫 1 个占位槽（`N = 8(L+1)`）。
   - `n` 为奇数：`L'` 必须是奇数——`L` 是奇数就直接用 `L`（`N = 8L`）；`L` 是偶数（包括 `L=0`，即没有局部变量）就要多垫 1 个占位槽（`N = 8(L+1)`）。
4. 只要这里一次性把 `N` 算对，函数体内后面所有 `call`（包括真实 ABI 调用）都自动保持 16 字节对齐，不需要在每个调用点再临时补 `sub rsp,8`/`add rsp,8`——前提是函数体内不再有 `begin`/局部变量声明之外的裸 `push`/`pop`（这套设计本来也不鼓励这么做，凡是要跨语句存活的值都应该是声明好的局部变量）。

下面 `assert(msg, flag)` 的例子里 `n=2`（偶数），只有 1 个局部变量 `result`（`L=1`，奇数），所以要多垫 1 个占位槽，`N = 8*(1+1) = 16`。

### 示例（`assert(msg, flag)`，2 个参数，1 个局部变量）

```asm
; 调用方：第一个参数先 push
push msg
push flag
call assert

; assert 内部：
assert:
    ;; params
    %define msg    [rbp + 24] ; 第 1 个参数，最先 push
    %define flag   [rbp + 16] ; 第 2 个参数，最后 push，离返回地址最近

    begin
    ;; local vars
    %define result [rbp - 8]  ; 第 1 个局部变量
    sub  rsp, 16 ;; n=2(偶) + L'=2(偶，1个real + 1个占位) 才对齐，见上面算法

    ; ... 用 result 存中间结果 ...
    mov rax, result ;; rax是返回值

    end
    ret  16    ; 清理调用方压入的 2 个参数（16 字节），局部变量随 end 里的 mov rsp,rbp 一并归还
```

单参数、无局部变量的函数（如下面例子里的 `fibo`、`print_num`）参数恒定落在 `[rbp+16]`；但按上面的对齐算法，`n=1`（奇数）时哪怕 `L=0` 也要垫 1 个占位槽（`sub rsp, 8`），不能因为"没有局部变量"就省掉 `sub rsp`。

## 跨真实 ABI 调用（如 `printf`）时的栈对齐

因为"调用会破坏所有寄存器"这条假设本来就统一适用于真实 ABI 调用，所以**不需要任何寄存器保护**——直接按 System V ABI 把参数放进 `rdi`/`rsi`/... 然后 `call` 即可。唯一还需要处理的是 System V ABI 对**调用点栈对齐**的要求：`call` 指令执行前 `rsp` 必须是 16 字节对齐。

只要在 `begin` 之后的那一次 `sub rsp, N`（见上面「按 16 字节对齐算」）里把 `n + L'` 的奇偶性算对，函数体内此后所有真实 ABI 调用天然就是对齐的，**不需要在每个 `call` 前再临时 `sub rsp,8` / 事后 `add rsp,8`**。这也是为什么哪怕函数完全没有局部变量（如 `print_num`），只要参数个数是奇数，也必须在 `begin` 之后老老实实 `sub rsp, 8` 垫一个占位槽——这个占位槽本质上就是在给之后的 `call printf` 找齐 16 字节对齐，只是把原来"临时在调用点垫、调用后立刻换回来"的做法，合并成了"函数入口一次性算好、`end` 里随 `mov rsp,rbp` 一并归还"。

## 完整调用示例（递归 fibo）

```asm
main:
    begin

    push 10          ; 传参：压栈
    call fibo
    push rax         ; fibo 返回值在 rax，作为参数传给 print_num
    call print_num
    ...

fibo:
    ;; params
    %define x   [rbp + 16]

    begin
    ;; local vars
    %define acc [rbp - 8]   ; 用来存第一次递归调用的结果
    sub  rsp, 8              ; n=1(奇) + L'=1(奇) 已对齐，不用额外占位槽

    mov rax, x
    cmp rax, 2
    jg .calc
    mov rax, 1
    jmp .end
.calc:
    mov rax, x
    sub rax, 1
    push rax
    call fibo        ; 调用之后除 rax 外所有寄存器视为已破坏
    mov acc, rax      ; 立刻把结果存回栈上的局部变量，不留在寄存器里

    mov rax, x
    sub rax, 2
    push rax
    call fibo
    add rax, acc      ; acc 是栈上变量，不受两次调用之间寄存器被破坏的影响
.end:
    end
    ret  8            ; 清理调用方压入的 1 个 8 字节参数（局部变量随 end 里的 mov rsp,rbp 一并归还）

print_num:
    ;; params
    %define x [rbp + 16]

    begin
    ;; 没有真正的局部变量，但 n=1(奇) + L'=0(偶) 不对齐，必须垫 1 个占位槽才能让后面 call printf 对齐
    sub rsp, 8

    mov rdi, print_msg   ; 按真实 System V ABI 准备 printf 的参数
    mov rsi, x
    xor rax, rax         ; printf 是变参函数，rax 需清零表示 0 个向量寄存器参数
    call printf           ; 无需任何寄存器保护——本来就没有寄存器里的值需要在调用后存活；
                           ; 栈对齐也已经在上面的 sub rsp,8 里一次性算好，这里不用再补

    end
    ret  8
```

## 这个模式解决的问题

写手工汇编时最烦的两件事：

1. 每个函数都要小心翼翼记住"我用了哪个寄存器、要不要保存、调用前后哪些寄存器还能信"——这套设计把答案统一成一句话："调用之后除 rax 外全部作废"，于是干脆不把任何东西放进寄存器里长期存活，问题直接消失，不需要再靠宏机械地保存/恢复。
2. 递归/多层调用时，寄存器容易在调用链上被不同层覆写而出 bug——把所有变量（参数和局部变量）都固定摆在栈上用 `%define` 访问，天然不受任何一次 `call` 影响，寄存器纯粹是"这一行算式"的草稿纸，用完即弃，从根源上不存在"寄存器被子调用覆写"这类 bug。

## 适用场景与局限

- 适合纯自研代码内部（函数间互相调用只走这套约定），把"要不要保存寄存器"这个问题直接消灭，心智负担比"callee-saved 约定 + 宏保存"更低。
- 边界清晰：一旦调用外部库（libc、系统调用等使用标准 ABI 的代码），只需按真实 ABI 把参数放进对应寄存器（如例中 `mov rdi, ...` / `mov rsi, ...`），不需要任何额外的寄存器保护步骤——因为本来就没有值指望在 `call` 之后还活在寄存器里；唯一要手动处理的是真实 ABI 要求的调用点 16 字节栈对齐。
- 代价：所有变量读写都要走一次内存访问（`[rbp±N]`），比全寄存器分配慢很多，也比"寄存器变量 + callee-saved 宏"的版本多一些 `mov`；仅适合教学/实验/把心智负担降到最低优先于性能的场景。


# 你的任务

基于这套自定义ABI，开发一个nasm用的汇编runtime，封装一些常用函数和系统调用。
